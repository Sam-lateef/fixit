import type { Href } from "expo-router";

/** Welcome screen: Google / Apple / phone (`app/auth/index.tsx`). Typed routes may lag until `expo start`. */
export const hrefAuthWelcome = "/auth" as unknown as Href;
