# Testing strategy — FixIt

**Mobile:** `mobile/` — `npx tsc --noEmit` from `mobile/`; manual flows on **dev build** for native modules (Google, purchases).

**API:** run project’s test script when present (`npm test` / `vitest` / etc. — document here when standardized).

**E2E:** (not yet defined — add when tooling is chosen)
