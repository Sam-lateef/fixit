# API base URL. Local dev: http://localhost:3000 (Fastify default). Prod: https://fixit-api.fly.dev
API_BASE=http://localhost:3000

# Fly: deploy from this folder — `fly apps create fixit-admin` once, then `fly deploy`.
# Production sets API_BASE via fly.toml [env] or `fly secrets set API_BASE=...`.
